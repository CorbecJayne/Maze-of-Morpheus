package gameElements;

import javax.swing.ImageIcon;

public class Wall extends Element {
	
	public Wall(int x, int y) {
		super(x, y);
		defaultIcon = new ImageIcon(getClass().getResource("Wand" + ".png"));
		id = 1;
	}

}